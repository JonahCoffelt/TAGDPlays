websockets>=13
pynput>=1.7

# Linux-only virtual keyboard. Windows and macOS use sendinput / quartz from the
# standard library instead, so this must not be installed there.
evdev>=1.7; sys_platform == "linux"
